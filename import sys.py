import sys
def intitial_phonebook():
    rows,cols = int(input( "Please enter initial number of contacts: "))
    phone_book = []
    print(phone_book)
    for i range in (rows):
        print("\n enter contact %d details in the following orders (ONLY): "
              % (i+1))
        print("NOTE:* indicators mandatory fields")
print(".............................................................................................")
        temp = []
        for j in range(cols)
        if temp [j] == " " or temp [j] == " ":
            sys.exist(
                "Name is a manditory field. process due to blank")
            if j == 2;
            temp.append(str(input("Enter email adress:")))
            if temp [j]
            